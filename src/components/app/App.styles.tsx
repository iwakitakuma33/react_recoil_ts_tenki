import { createStyles, makeStyles } from '@material-ui/core';

export const useStyles = makeStyles(() =>
  createStyles({
    pageContent: {
      paddingTop: 12,
      paddingBottom: 12,
    },
  })
);
